local Vendor = script.Vendor
local Utility = script.Utility
local Components = script.Components

local Roact = require(Vendor.Roact)

local function WelcomePage(props, hooks)
	local page, setPage = hooks.useState("Home")
end